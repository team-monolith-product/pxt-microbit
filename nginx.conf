server {
    listen 4000;
    server_name localhost;

    root /usr/share/nginx/html;

    location / {
        gzip_static on;
        # CloudFront 등 프록시 경유 요청(Via 헤더)에도 사전압축 .gz를 서빙 (기본 off면 gzip_static이 프록시 요청에 .gz 미서빙)
        gzip_proxied any;
        try_files $uri /index.html;
    }
}
